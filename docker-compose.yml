version: "2"
services:
  mydb:
    image: postgres
    environment:
      POSTGRES_PASSWORD: fantasy
      POSTGRES_USER: fantasy
      POSTGRES_DB: fantasy
  server:
    build: ./
    volumes:
      - ./coverage:/coverage-out
    command: bash -c "scripts/test.sh"
    depends_on:
      - mydb